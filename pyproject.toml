[project]
name = "projects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "somin6241", email = "somine0904@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.6",
    "plotly>=7.1.0",
    "seaborn>=0.13.2",
    "streamlit>=1.64.0",
]

[project.scripts]
projects = "projects:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
